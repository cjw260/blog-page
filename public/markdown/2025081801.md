---
title: "vite的基本使用"
date: 2025-08-18
category: "技术文章"
tags: ["vite"]
summary: "说明 Vite 基于 ESM 直接保持模块化、省去打包编译从而获得极快热更新的特点，并给出 vite.config.js 中 resolve、build.assetsInlineLimit、server.port 与 base 四项常用配置的示例。"
updated_at: "2026-09-17T20:26:16+08:00"
---
# vite的基本使用

vite的最大特点是利用esm，让代码不像传统的构建工具一样去分析引入，打包构建，而是直接保持模块化，这样省去了大量的编译时间，让代码更改后的响应速度大量提升

基本结构，vite的配置写在vite.config.js中

```js
import { defineConfig } from "vite";
export default defineConfig({
    resolve:{},//用来写文件后缀省略或者别名的地方
    build: {
        assetsInlineLimit: 20000//小于20000图片转成base64
    },
    server:{//开发模式配置
        port: 2000,//端口号
    },
    base: "www.xxx.com"//类似于webpack的public path
})
```
